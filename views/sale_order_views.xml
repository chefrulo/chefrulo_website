<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Show delivery date and shipping note on sale order form -->
    <record id="sale_order_delivery_info_view" model="ir.ui.view">
        <field name="name">sale.order.delivery.info</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_confirm']" position="after">
                <button name="action_send_payment_request"
                        string="Send Payment Request"
                        type="object"
                        class="btn-secondary"
                        invisible="state not in ('sale', 'done')"/>
            </xpath>
            <xpath expr="//field[@name='note']" position="before">
                <group string="Delivery Info" col="2">
                    <field name="delivery_date" readonly="1" invisible="not delivery_date"/>
                    <field name="shipping_note" readonly="1" invisible="not shipping_note"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
